# Left side (central) configuration
CONFIG_ZMK_KEYBOARD_NAME="AOKB"

# Enable serial for wired-split communication
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y

# Enable USB on central side
CONFIG_ZMK_USB=y
